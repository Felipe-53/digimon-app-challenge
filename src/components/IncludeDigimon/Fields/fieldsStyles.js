import {makeStyles} from '@material-ui/core/styles';

const useStyles = makeStyles({
  nameField: {
    width: '12rem',
    margin: '10% 0',
  },

  levelField: {
    width: '12rem',
    margin: '10% 0',
  },

});

export default useStyles;