import {makeStyles} from '@material-ui/core/styles';

const useStyles = makeStyles({
  inputs: {
    width: '12rem',
    // marginTop: '1rem',
    marginBottom: '1.5rem'
  },

  radioGroup: {
    maxWidth: '15rem',
    margin: '2rem',
  }
});

export default useStyles;